From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: deepak1556 <hop2deep@gmail.com>
Date: Mon, 5 Sep 2022 19:07:54 +0900
Subject: fix: return v8::Value from LocalFrame::RequestExecuteScript

Allows api::WebFrame::ExecuteJavaScript(InIsolateWorld) to work with
v8::Value instead of base::Value, and reports the rejection reason of an
awaited promise result to that callback instead of dropping it.
Refs https://bugs.chromium.org/p/chromium/issues/detail?id=1323953

diff --git a/extensions/renderer/script_injection.cc b/extensions/renderer/script_injection.cc
index b019a321f1445d92cb433fd9ffe41d3fe0eb8644..ec2efb4d04fe5f76c65418bfc03a5bdb40a09cf9 100644
--- a/extensions/renderer/script_injection.cc
+++ b/extensions/renderer/script_injection.cc
@@ -9,6 +9,7 @@
 
 #include "base/feature_list.h"
 #include "base/functional/bind.h"
+#include "base/functional/callback_helpers.h"
 #include "base/lazy_instance.h"
 #include "base/memory/raw_ptr.h"
 #include "base/metrics/histogram_macros.h"
@@ -327,6 +328,7 @@ void ScriptInjection::InjectJs(std::set<std::string>* executing_scripts,
       blink::mojom::LoadEventBlockingOption::kBlock,
       base::BindOnce(&ScriptInjection::OnJsInjectionCompleted,
                      weak_ptr_factory_.GetWeakPtr()),
+      base::NullCallback(),
       blink::BackForwardCacheAware::kPossiblyDisallow,
       injector_->ExpectsResults(), injector_->ShouldWaitForPromise(),
       script_injector_id);
diff --git a/third_party/blink/public/web/web_local_frame.h b/third_party/blink/public/web/web_local_frame.h
index 5bf29ef3337282d8d019ff164194ffcf79901528..c865e4fca0fe80470d78f55aaa5649a90f16d10c 100644
--- a/third_party/blink/public/web/web_local_frame.h
+++ b/third_party/blink/public/web/web_local_frame.h
@@ -468,6 +468,7 @@ class BLINK_EXPORT WebLocalFrame : public WebFrame {
                                     mojom::EvaluationTiming,
                                     mojom::LoadEventBlockingOption,
                                     WebScriptExecutionCallback,
+                                    WebScriptExecutionCallbackUnmodified,
                                     BackForwardCacheAware,
                                     mojom::WantResultOption,
                                     mojom::PromiseResultOption,
diff --git a/third_party/blink/public/web/web_script_execution_callback.h b/third_party/blink/public/web/web_script_execution_callback.h
index cba373664bec3a32abad6fe0396bd67b53b7e67f..13284f693f406cea171e450af7784a4425c8d690 100644
--- a/third_party/blink/public/web/web_script_execution_callback.h
+++ b/third_party/blink/public/web/web_script_execution_callback.h
@@ -14,6 +14,12 @@ class TimeTicks;
 class Value;
 }
 
+namespace v8 {
+class Value;
+template <class T>
+class Local;
+}
+
 namespace blink {
 
 // Non-nullopt `base::Value` is passed to the callback if
@@ -43,6 +49,14 @@ namespace blink {
 using WebScriptExecutionCallback =
     base::OnceCallback<void(std::optional<base::Value>, base::TimeTicks)>;
 
+// Receives the scripts' v8 completion values without conversion. When
+// promise results are awaited, a script whose promise was rejected has an
+// empty value in `results` and the rejection reason at the same index of
+// `rejections`; otherwise `rejections` holds empty values.
+using WebScriptExecutionCallbackUnmodified = base::OnceCallback<void(
+    const std::vector<v8::Local<v8::Value>>& results,
+    const std::vector<v8::Local<v8::Value>>& rejections)>;
+
 }  // namespace blink
 
 #endif  // THIRD_PARTY_BLINK_PUBLIC_WEB_WEB_SCRIPT_EXECUTION_CALLBACK_H_
diff --git a/third_party/blink/renderer/core/frame/local_frame.cc b/third_party/blink/renderer/core/frame/local_frame.cc
index ce332f97dd089cb155f9977158a2c6ed6544b923..c1278956cd2e25acdeaf1f210431225070e6e264 100644
--- a/third_party/blink/renderer/core/frame/local_frame.cc
+++ b/third_party/blink/renderer/core/frame/local_frame.cc
@@ -3394,6 +3394,7 @@ void LocalFrame::RequestExecuteScript(
     mojom::blink::EvaluationTiming evaluation_timing,
     mojom::blink::LoadEventBlockingOption blocking_option,
     WebScriptExecutionCallback callback,
+    WebScriptExecutionCallbackUnmodified raw_callback,
     BackForwardCacheAware back_forward_cache_aware,
     mojom::blink::WantResultOption want_result_option,
     mojom::blink::PromiseResultOption promise_behavior,
@@ -3453,7 +3454,8 @@ void LocalFrame::RequestExecuteScript(
   PausableScriptExecutor::CreateAndRun(
       script_state, std::move(script_sources), execute_script_policy,
       user_gesture, evaluation_timing, blocking_option, want_result_option,
-      promise_behavior, std::move(callback), script_injector_id);
+      promise_behavior, std::move(callback), std::move(raw_callback),
+      script_injector_id);
 }
 
 void LocalFrame::SetEvictCachedSessionStorageOnFreezeOrUnload() {
diff --git a/third_party/blink/renderer/core/frame/local_frame.h b/third_party/blink/renderer/core/frame/local_frame.h
index c9f63eadd7bda3d9c5e73cdd856911167b3220ec..febaf3dc37ad4fd5addf187c857ecdaab7a603a4 100644
--- a/third_party/blink/renderer/core/frame/local_frame.h
+++ b/third_party/blink/renderer/core/frame/local_frame.h
@@ -871,6 +871,7 @@ class CORE_EXPORT LocalFrame final
                             mojom::blink::EvaluationTiming,
                             mojom::blink::LoadEventBlockingOption,
                             WebScriptExecutionCallback,
+                            WebScriptExecutionCallbackUnmodified,
                             BackForwardCacheAware back_forward_cache_aware,
                             mojom::blink::WantResultOption,
                             mojom::blink::PromiseResultOption,
diff --git a/third_party/blink/renderer/core/frame/local_frame_mojo_handler.cc b/third_party/blink/renderer/core/frame/local_frame_mojo_handler.cc
index d0b9b0815dbf3bbf39a4a1fbf9b886b45bac0da9..1a6d94940886b8fc0a1a42e8ab3c299746efb761 100644
--- a/third_party/blink/renderer/core/frame/local_frame_mojo_handler.cc
+++ b/third_party/blink/renderer/core/frame/local_frame_mojo_handler.cc
@@ -976,6 +976,7 @@ void LocalFrameMojoHandler::JavaScriptExecuteRequestInIsolatedWorld(
             std::move(callback).Run(value ? std::move(*value) : base::Value());
           },
           std::move(callback)),
+      base::NullCallback(),
       BackForwardCacheAware::kAllow,
       wants_result
           ? mojom::blink::WantResultOption::kWantResultDateAndRegExpAllowed
diff --git a/third_party/blink/renderer/core/frame/pausable_script_executor.cc b/third_party/blink/renderer/core/frame/pausable_script_executor.cc
index b47588db64b635eb48addd92ab074aaa455804aa..40d66f2f3f570d46040fa756c61f399bab4761fd 100644
--- a/third_party/blink/renderer/core/frame/pausable_script_executor.cc
+++ b/third_party/blink/renderer/core/frame/pausable_script_executor.cc
@@ -10,6 +10,7 @@
 
 #include "base/compiler_specific.h"
 #include "base/functional/callback.h"
+#include "base/functional/callback_helpers.h"
 #include "base/logging.h"
 #include "base/numerics/safe_conversions.h"
 #include "third_party/blink/public/mojom/frame/user_activation_notification_type.mojom-blink.h"
@@ -43,13 +44,19 @@ namespace {
 // then-able).
 class PromiseAggregator : public GarbageCollected<PromiseAggregator> {
  public:
-  using Callback = base::OnceCallback<void(const v8::LocalVector<v8::Value>&)>;
+  // Receives the fulfilled values and, for each value that was rejected
+  // instead, the rejection reason at the same index of the second vector.
+  using Callback = base::OnceCallback<void(const v8::LocalVector<v8::Value>&,
+                                           const v8::LocalVector<v8::Value>&)>;
 
   PromiseAggregator(ScriptState* script_state,
                     const v8::LocalVector<v8::Value>& values,
                     Callback callback);
 
-  void Trace(Visitor* visitor) const { visitor->Trace(results_); }
+  void Trace(Visitor* visitor) const {
+    visitor->Trace(results_);
+    visitor->Trace(rejections_);
+  }
 
  private:
   // A helper class that handles a result from a single promise value.
@@ -71,6 +78,9 @@ class PromiseAggregator : public GarbageCollected<PromiseAggregator> {
       if (was_fulfilled_) {
         aggregator_->results_[index_].Reset(script_state->GetIsolate(),
                                             value.V8Value());
+      } else {
+        aggregator_->rejections_[index_].Reset(script_state->GetIsolate(),
+                                               value.V8Value());
       }
 
       if (--aggregator_->outstanding_ == 0) {
@@ -94,6 +104,8 @@ class PromiseAggregator : public GarbageCollected<PromiseAggregator> {
 
   // The accumulated vector of results from the promises.
   HeapVector<TraceWrapperV8Reference<v8::Value>> results_;
+  // The rejection reasons of the promises that were rejected.
+  HeapVector<TraceWrapperV8Reference<v8::Value>> rejections_;
   // The number of outstanding promises we're waiting on.
   wtf_size_t outstanding_ = 0;
   // The callback to invoke when all promises are settled.
@@ -104,6 +116,7 @@ PromiseAggregator::PromiseAggregator(ScriptState* script_state,
                                      const v8::LocalVector<v8::Value>& values,
                                      Callback callback)
     : results_(static_cast<wtf_size_t>(values.size())),
+      rejections_(static_cast<wtf_size_t>(values.size())),
       callback_(std::move(callback)) {
   for (wtf_size_t i = 0; i < values.size(); ++i) {
     if (values[i].IsEmpty())
@@ -128,10 +141,14 @@ PromiseAggregator::PromiseAggregator(ScriptState* script_state,
 void PromiseAggregator::OnAllSettled(v8::Isolate* isolate) {
   DCHECK_EQ(0u, outstanding_);
   v8::LocalVector<v8::Value> converted_results(isolate, results_.size());
-  for (wtf_size_t i = 0; i < results_.size(); ++i)
+  v8::LocalVector<v8::Value> converted_rejections(isolate, results_.size());
+  for (wtf_size_t i = 0; i < results_.size(); ++i) {
     converted_results[i] = results_[i].Get(isolate);
+    converted_rejections[i] = rejections_[i].Get(isolate);
+  }
 
-  std::move(callback_).Run(std::move(converted_results));
+  std::move(callback_).Run(std::move(converted_results),
+                           std::move(converted_rejections));
 }
 
 class WebScriptExecutor : public PausableScriptExecutor::Executor {
@@ -249,7 +266,7 @@ void PausableScriptExecutor::CreateAndRun(
           script_state, mojom::blink::UserActivationOption::kDoNotActivate,
           mojom::blink::LoadEventBlockingOption::kDoNotBlock,
           want_result_option, mojom::blink::PromiseResultOption::kDoNotWait,
-          std::move(callback),
+          std::move(callback), base::NullCallback(),
           MakeGarbageCollected<V8FunctionExecutor>(isolate, function, receiver,
                                                    argc, argv),
           /*script_injector_id=*/String());
@@ -266,10 +283,11 @@ void PausableScriptExecutor::CreateAndRun(
     mojom::blink::WantResultOption want_result_option,
     mojom::blink::PromiseResultOption promise_result_option,
     WebScriptExecutionCallback callback,
+    WebScriptExecutionCallbackUnmodified raw_callback,
     const String& script_injector_id) {
   auto* executor = MakeGarbageCollected<PausableScriptExecutor>(
       script_state, user_activation_option, blocking_option, want_result_option,
-      promise_result_option, std::move(callback),
+      promise_result_option, std::move(callback), std::move(raw_callback),
       MakeGarbageCollected<WebScriptExecutor>(std::move(sources),
                                               execute_script_policy),
       script_injector_id);
@@ -292,6 +310,16 @@ void PausableScriptExecutor::ContextDestroyed() {
     ScriptState::Scope script_scope(script_state_);
     std::move(callback_).Run({}, {});
   }
+  if (raw_callback_) {
+    // Though the context is (about to be) destroyed, the callback is invoked
+    // with a vector of v8::Local<>s, which implies that creating v8::Locals
+    // is permitted. Ensure a valid scope is present for the callback.
+    // See https://crbug.com/840719.
+    ScriptState::Scope script_scope(script_state_);
+    std::move(raw_callback_)
+        .Run(std::vector<v8::Local<v8::Value>>(),
+             std::vector<v8::Local<v8::Value>>());
+  }
   Dispose();
 }
 
@@ -302,11 +330,13 @@ PausableScriptExecutor::PausableScriptExecutor(
     mojom::blink::WantResultOption want_result_option,
     mojom::blink::PromiseResultOption promise_result_option,
     WebScriptExecutionCallback callback,
+    WebScriptExecutionCallbackUnmodified raw_callback,
     Executor* executor,
     const String& script_injector_id)
     : ExecutionContextLifecycleObserver(ExecutionContext::From(script_state)),
       script_state_(script_state),
       callback_(std::move(callback)),
+      raw_callback_(std::move(raw_callback)),
       user_activation_option_(user_activation_option),
       blocking_option_(blocking_option),
       want_result_option_(want_result_option),
@@ -401,13 +431,16 @@ void PausableScriptExecutor::ExecuteAndDestroySelf() {
       break;
 
     case mojom::blink::PromiseResultOption::kDoNotWait:
-      HandleResults(results);
+      HandleResults(results,
+                    v8::LocalVector<v8::Value>(script_state_->GetIsolate(),
+                                               results.size()));
       break;
   }
 }
 
 void PausableScriptExecutor::HandleResults(
-    const v8::LocalVector<v8::Value>& results) {
+    const v8::LocalVector<v8::Value>& results,
+    const v8::LocalVector<v8::Value>& rejections) {
   // The script may have removed the frame, in which case ContextDestroyed()
   // will have handled the disposal/callback.
   if (!script_state_->ContextIsValid())
@@ -446,6 +479,15 @@ void PausableScriptExecutor::HandleResults(
     std::move(callback_).Run(std::move(value), start_time_);
   }
 
+  if (raw_callback_) {
+    // TODO: use `v8::LocalVector` instead of `std::vector<v8::Local<...>>`
+    // (see https://crbug.com/40272194)
+    std::move(raw_callback_)
+        .Run(std::vector<v8::Local<v8::Value>>(results.begin(), results.end()),
+             std::vector<v8::Local<v8::Value>>(rejections.begin(),
+                                               rejections.end()));
+  }
+
   Dispose();
 }
 
diff --git a/third_party/blink/renderer/core/frame/pausable_script_executor.h b/third_party/blink/renderer/core/frame/pausable_script_executor.h
index 4ed8807eadcbdef7c77cb06fa072f90e216b9d2d..d12223bb29774d75e78380887299e831a6d4fa4f 100644
--- a/third_party/blink/renderer/core/frame/pausable_script_executor.h
+++ b/third_party/blink/renderer/core/frame/pausable_script_executor.h
@@ -48,6 +48,7 @@ class CORE_EXPORT PausableScriptExecutor final
                            mojom::blink::WantResultOption,
                            mojom::blink::PromiseResultOption,
                            WebScriptExecutionCallback,
+                           WebScriptExecutionCallbackUnmodified,
                            const String& script_injector_id);
 
   class Executor : public GarbageCollected<Executor> {
@@ -65,6 +66,7 @@ class CORE_EXPORT PausableScriptExecutor final
                          mojom::blink::WantResultOption,
                          mojom::blink::PromiseResultOption,
                          WebScriptExecutionCallback,
+                         WebScriptExecutionCallbackUnmodified,
                          Executor*,
                          const String& script_injector_id);
   ~PausableScriptExecutor() override;
@@ -80,10 +82,12 @@ class CORE_EXPORT PausableScriptExecutor final
   void ExecuteAndDestroySelf();
   void Dispose();
 
-  void HandleResults(const v8::LocalVector<v8::Value>& results);
+  void HandleResults(const v8::LocalVector<v8::Value>& results,
+                     const v8::LocalVector<v8::Value>& rejections);
 
   Member<ScriptState> script_state_;
   WebScriptExecutionCallback callback_;
+  WebScriptExecutionCallbackUnmodified raw_callback_;
   base::TimeTicks start_time_;
   const mojom::blink::UserActivationOption user_activation_option_;
   const mojom::blink::LoadEventBlockingOption blocking_option_;
diff --git a/third_party/blink/renderer/core/frame/web_frame_test.cc b/third_party/blink/renderer/core/frame/web_frame_test.cc
index 5215b956563b8c15bbbc74da76348881b9fd287c..3c29306aa2c3deae50f1719c39bdd5c1a407a3ad 100644
--- a/third_party/blink/renderer/core/frame/web_frame_test.cc
+++ b/third_party/blink/renderer/core/frame/web_frame_test.cc
@@ -308,6 +308,7 @@ void ExecuteScriptsInMainWorld(
       DOMWrapperWorld::kMainWorldId, sources, user_gesture,
       mojom::blink::EvaluationTiming::kSynchronous,
       mojom::blink::LoadEventBlockingOption::kDoNotBlock, std::move(callback),
+      base::NullCallback(),
       BackForwardCacheAware::kAllow,
       mojom::blink::WantResultOption::kWantResult, wait_for_promise,
       /*script_injector_id=*/WebString());
diff --git a/third_party/blink/renderer/core/frame/web_local_frame_impl.cc b/third_party/blink/renderer/core/frame/web_local_frame_impl.cc
index 90a33030baaa2ae311b41902a1d77055b26799bc..ee80da9f785f1197b927acf7b0296e9e498287a3 100644
--- a/third_party/blink/renderer/core/frame/web_local_frame_impl.cc
+++ b/third_party/blink/renderer/core/frame/web_local_frame_impl.cc
@@ -1141,6 +1141,7 @@ void WebLocalFrameImpl::RequestExecuteScript(
     mojom::blink::EvaluationTiming evaluation_timing,
     mojom::blink::LoadEventBlockingOption blocking_option,
     WebScriptExecutionCallback callback,
+    WebScriptExecutionCallbackUnmodified raw_callback,
     BackForwardCacheAware back_forward_cache_aware,
     mojom::blink::WantResultOption want_result_option,
     mojom::blink::PromiseResultOption promise_behavior,
@@ -1148,8 +1149,8 @@ void WebLocalFrameImpl::RequestExecuteScript(
   DCHECK(GetFrame());
   GetFrame()->RequestExecuteScript(
       world_id, sources, user_gesture, evaluation_timing, blocking_option,
-      std::move(callback), back_forward_cache_aware, want_result_option,
-      promise_behavior, script_injector_id);
+      std::move(callback), std::move(raw_callback), back_forward_cache_aware,
+      want_result_option, promise_behavior, script_injector_id);
 }
 
 bool WebLocalFrameImpl::IsInspectorConnected() {
diff --git a/third_party/blink/renderer/core/frame/web_local_frame_impl.h b/third_party/blink/renderer/core/frame/web_local_frame_impl.h
index eb10e7107867a2f1b20d57c9eca23a8af92c88c2..69e853d0e2389f86f37492d974415eede9f519d9 100644
--- a/third_party/blink/renderer/core/frame/web_local_frame_impl.h
+++ b/third_party/blink/renderer/core/frame/web_local_frame_impl.h
@@ -201,6 +201,7 @@ class CORE_EXPORT WebLocalFrameImpl final
                             mojom::blink::EvaluationTiming,
                             mojom::blink::LoadEventBlockingOption,
                             WebScriptExecutionCallback,
+                            WebScriptExecutionCallbackUnmodified,
                             BackForwardCacheAware back_forward_cache_aware,
                             mojom::blink::WantResultOption,
                             mojom::blink::PromiseResultOption,
