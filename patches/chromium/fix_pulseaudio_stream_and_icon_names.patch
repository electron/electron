From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Damglador <vse.stopchanskyi@gmail.com>
Date: Fri, 26 Dec 2025 21:26:43 +0100
Subject: fix: pulseaudio stream and icon names

Read the PulseAudio context name from ELECTRON_PA_APP_NAME and the stream
icon name from ELECTRON_PA_ICON_NAME, which Electron's browser process exports
before any audio starts (child processes inherit them), falling back to the
upstream PRODUCT_STRING / kBrowserDisplayName values when they are unset.

This replaces the hard-coded names so that audio streams are recognizable in
tools like qpwgraph and desktop audio managers, instead of every Electron app
showing up as "Chromium" / "Chromium input".

This patch can be removed when upstream starts using AudioManager::SetGlobalAppName()
for all pa_context names (and when actually works with AudioServiceOutOfProcess).

diff --git a/media/audio/pulse/pulse_util.cc b/media/audio/pulse/pulse_util.cc
index fd1fea63ea424b7e90daf0e815e3c2631f0e6c6b..f535c3a1c829ce971b406ea4c828adc32bf89b99 100644
--- a/media/audio/pulse/pulse_util.cc
+++ b/media/audio/pulse/pulse_util.cc
@@ -5,6 +5,7 @@
 #include "media/audio/pulse/pulse_util.h"
 
 #include <stdint.h>
+#include <stdlib.h>
 #include <string.h>
 
 #include <memory>
@@ -43,6 +44,18 @@ constexpr char kBrowserDisplayName[] = "chromium-browser";
 #define PRODUCT_STRING "Chromium"
 #endif
 
+// The embedder exports these before audio starts so PulseAudio clients and
+// streams carry the application's name and icon instead of the browser's.
+const char* ContextName(const char* fallback) {
+  const char* name = getenv("ELECTRON_PA_APP_NAME");
+  return name && *name ? name : fallback;
+}
+
+const char* IconName() {
+  const char* name = getenv("ELECTRON_PA_ICON_NAME");
+  return name && *name ? name : kBrowserDisplayName;
+}
+
 #if defined(DLOPEN_PULSEAUDIO)
 static const base::FilePath::CharType kPulseLib[] =
     FILE_PATH_LITERAL("libpulse.so.0");
@@ -250,7 +263,7 @@ bool InitPulse(pa_threaded_mainloop** mainloop, pa_context** context) {
 
   pa_mainloop_api* pa_mainloop_api = pa_threaded_mainloop_get_api(pa_mainloop);
   pa_context* pa_context =
-      pa_context_new(pa_mainloop_api, PRODUCT_STRING " input");
+      pa_context_new(pa_mainloop_api, ContextName(PRODUCT_STRING " input"));
   if (!pa_context) {
     pa_threaded_mainloop_free(pa_mainloop);
     return false;
@@ -479,7 +492,7 @@ bool CreateInputStream(pa_threaded_mainloop* mainloop,
   // tells PulseAudio what the stream icon should be.
   ScopedPropertyList property_list;
   pa_proplist_sets(property_list.get(), PA_PROP_APPLICATION_ICON_NAME,
-                   kBrowserDisplayName);
+                   IconName());
   *stream = pa_stream_new_with_proplist(context, "RecordStream",
                                         &sample_specifications, map,
                                         property_list.get());
@@ -540,7 +553,8 @@ bool CreateOutputStream(raw_ptr<pa_threaded_mainloop>* mainloop,
 
   pa_mainloop_api* pa_mainloop_api = pa_threaded_mainloop_get_api(*mainloop);
   *context = pa_context_new(
-      pa_mainloop_api, app_name.empty() ? PRODUCT_STRING : app_name.c_str());
+      pa_mainloop_api,
+      ContextName(app_name.empty() ? PRODUCT_STRING : app_name.c_str()));
   RETURN_ON_FAILURE(*context, "Failed to create PulseAudio context.");
 
   // A state callback must be set before calling pa_threaded_mainloop_lock() or
@@ -589,7 +603,7 @@ bool CreateOutputStream(raw_ptr<pa_threaded_mainloop>* mainloop,
   // tell PulseAudio what the stream icon should be.
   ScopedPropertyList property_list;
   pa_proplist_sets(property_list.get(), PA_PROP_APPLICATION_ICON_NAME,
-                   kBrowserDisplayName);
+                   IconName());
   *stream = pa_stream_new_with_proplist(
       *context, "Playback", &sample_specifications, map, property_list.get());
   RETURN_ON_FAILURE(*stream, "failed to create PA playback stream");
