name: 'Checkout'
description: 'Checks out Electron and stores it in the AKS Cache'

# Thin serial wrapper over the small checkout-* composites (checkout-prepare,
# install-dependencies, install-build-tools, checkout-generate-sas,
# checkout-restore-gitcache, checkout-sync, checkout-wait-ssh) so every step
# body lives in exactly one place. The build.yml checkout jobs use the same
# composites via .github/workflows/pipeline-segment-electron-checkout.yml,
# where the mutually independent pre-sync composites run as parallel
# background steps instead of serially.

inputs:
  generate-sas-token:
    description: 'Whether to generate and persist a SAS token for the item in the cache'
    required: false
    default: 'false'
  use-cache:
    description: 'Whether to persist the cache to the shared drive'
    required: false
    default: 'true'
  target-platform:
    description: 'Target platform, should be linux, win, macos'
  is-release:
    description: 'Whether this checkout is for a release build'
    required: false
    default: 'false'
runs:
  using: "composite"
  steps:
  - name: Prepare Checkout
    id: prepare
    uses: ./src/electron/.github/actions/checkout-prepare
    with:
      target-platform: ${{ inputs.target-platform }}
      use-cache: ${{ inputs.use-cache }}
  - name: Install Dependencies
    uses: ./src/electron/.github/actions/install-dependencies
  - name: Install Build Tools
    uses: ./src/electron/.github/actions/install-build-tools
  - name: Generate SAS Key
    if: ${{ inputs.generate-sas-token == 'true' }}
    uses: ./src/electron/.github/actions/checkout-generate-sas
    with:
      target-platform: ${{ inputs.target-platform }}
  - name: Restore gitcache
    if: steps.prepare.outputs.cache-exists == 'false'
    uses: ./src/electron/.github/actions/checkout-restore-gitcache
  - name: Sync and Save Cache
    uses: ./src/electron/.github/actions/checkout-sync
    with:
      target-platform: ${{ inputs.target-platform }}
      generate-sas-token: ${{ inputs.generate-sas-token }}
      use-cache: ${{ inputs.use-cache }}
      is-release: ${{ inputs.is-release }}
      cache-exists: ${{ steps.prepare.outputs.cache-exists }}
  - name: Wait for active SSH sessions
    if: always() && !cancelled()
    uses: ./src/electron/.github/actions/checkout-wait-ssh
