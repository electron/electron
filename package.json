{
  "name": "electron",
  "version": "0.36.3",
  "devDependencies": {
    "asar": "^0.9.0",
    "request": "*"
  },
  "optionalDependencies": {
    "runas": "^3.0.0"
  },
  "private": true,
  "scripts": {
    "preinstall": "node -e 'process.exit(0)'",
    "test": "python ./script/test.py"
  }
}
