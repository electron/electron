{
  # If it looks stupid but it works it ain't stupid.
  'variables': {
    'variables': {
      'enable_osr%': 1,
      'enable_certificate_viewer%': 0,
    },
    'enable_osr%': '<(enable_osr)',
    'enable_certificate_viewer%': '<(enable_certificate_viewer)',
  },
}
